{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^db:generate"],
      "outputs": ["dist/**"]
    },
    "db:generate": {
      "outputs": ["prisma/client/**"]
    },
    "lint": {},
    "lint:fix": {},
    "format": {},
    "format:fix": {},
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
